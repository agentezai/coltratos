status: approved
created: "2026-04-26"
approved_at: "2026-05-04"
action_type: FEATURE
profile: code
updated: "2026-05-04"
revision_count: 4
last_revision: "Rev 4 — Re-plan to align with mvp-scope §59 + queue-fed pipeline. Pure function → queue-fed worker (entrypoint side-effectful in src/services/ingestion-worker/, inner pipeline pure in lib/ingestion/). Adds OCR (Tesseract spa) + library-based tables (pdfplumber subprocess) + empty-page flagging as in-scope. ADR-005 superseded by ADR-010 (queue-worker boundary). New ADRs: 008 (pdfplumber subprocess), 009 (Railway/Fly.io), 010 (worker boundary), 011 (writeback via repository port), 012 (Supabase pgmq + pg-boss fallback note). T8 idempotency expanded verbatim on pliego_upload_id (status upsert + page upsert + status check at entry + concurrency control via pgmq visibility timeout OR advisory lock). NFR-03 scope lib/ingestion/** reaffirmed (worker exempt by design). T0 satisfied externally by domain-model-mvp rev 1 (pliego_uploads ingestion cols + pdf_pages table + ADR-013). Eval corpus N=20 / <5% page-failure / p95 <2 min on 200 pages. Cross-spec follow-up: requisitos-extraction RN-012 (synthetic + general filter) is obsolete and must be retired in a separate Sprint 3 edit session — captured as TODO in this rev's deltas. Paused/resumed sequence: rev 3 approved 2026-04-27; mvp-scope.md consolidated 2026-04-28; pilot-research 2026-05-04 established queue-fed pipeline; storage-shape decision per-page rows; cross-spec follow-ups tracked. All 15 prior task checkboxes were unchecked at re-plan time; no work lost."
