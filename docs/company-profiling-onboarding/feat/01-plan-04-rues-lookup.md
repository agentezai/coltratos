# T4: REMOVED — RUES Lookup Service

**Removed from MVP scope** (2026-05-05).

RUES API auto-fill was not included in the MVP discovery answers. Manual NIT entry is sufficient; the form validates the dígito de verificación server-side (Modulo 11) without requiring a RUES lookup.

See `spec.md` revision log for rationale.
